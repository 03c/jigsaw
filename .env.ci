# CI / docker-compose.ci.yml — committed for reproducible automation (non-secret dev values only)
NODE_ENV=production
PANEL_DOMAIN=localhost:3000
KEYCLOAK_CLIENT_SECRET=ci-client-secret
JIGSAW_ADMIN_EMAIL=ci@localhost
KEYCLOAK_ADMIN_PASSWORD=admin
